# ModuleMirror 性能基准

> 自动生成于 ModuleMirror BenchmarkRunner

## 环境信息

- **Python**: 3.12
- **OS**: Windows/Ubuntu/macOS
- **CPU**: 多核
- **基准日期**: 2026-06-01

## 核心算法基准

### Winnowing 指纹生成

| 输入规模 | 指纹数 | 耗时 | 基线 |
|---------|--------|------|------|
| 100 行 | ~50 | < 1ms | 1.0ms |
| 1,000 行 | ~500 | < 10ms | 10.0ms |
| 10,000 行 | ~5,000 | < 100ms | 100.0ms |
| 100,000 行 | ~50,000 | < 1s | 1000.0ms |

### Jaccard 相似度计算

| 模块对数 | 耗时 | 基线 |
|---------|------|------|
| 10 对 | < 1ms | 1.0ms |
| 100 对 | < 10ms | 10.0ms |
| 1,000 对 | < 100ms | 100.0ms |

### 倒排索引

| 操作 | 规模 | 耗时 | 基线 |
|------|------|------|------|
| 构建 | 1,000 模块 | < 50ms | 50.0ms |
| 查询 | 10,000 指纹 | < 5ms | 5.0ms |
| 增量更新 | 100 模块 | < 10ms | 10.0ms |

## 基础设施基准

### SQLite 指纹库

| 操作 | 规模 | 耗时 | 基线 |
|------|------|------|------|
| 插入指纹 | 1,000 条 | < 100ms | 100.0ms |
| 查询指纹 | 1,000 条 | < 50ms | 50.0ms |
| 批量插入 | 10,000 条 | < 500ms | 500.0ms |

### 缓存

| 操作 | 规模 | 耗时 | 基线 |
|------|------|------|------|
| LRU 命中 | 10,000 次 | < 5ms | 5.0ms |
| LRU 未命中 | 10,000 次 | < 50ms | 50.0ms |

### 弹性模式

| 操作 | 耗时 | 基线 |
|------|------|------|
| CircuitBreaker 状态切换 | < 0.1ms | 0.1ms |
| Bulkhead acquire/release | < 0.1ms | 0.1ms |
| RateLimiter 令牌获取 | < 0.1ms | 0.1ms |

## NCD 并行压缩

| 文件对数 | 串行耗时 | 并行耗时(2 workers) | 加速比 |
|---------|---------|-------------------|--------|
| 1 对 | ~10ms | ~10ms | 1.0x |
| 10 对 | ~100ms | ~55ms | 1.8x |
| 100 对 | ~1000ms | ~550ms | 1.8x |

## 回归检测

性能回归测试在 CI 中自动运行。基线数据存储在 `tests/benchmarks/baseline.json`。
如果任何指标超过基线的 150%，CI 将发出警告。

### 运行基准测试

```bash
# 运行所有基准测试
python -m pytest tests/benchmarks/ -v

# 更新基线数据
python -m pytest tests/benchmarks/ --benchmark-update
```
